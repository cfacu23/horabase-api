spring.application.name=horabase-api

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/horabase}
spring.datasource.username=${DB_USERNAME:horabase}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}
spring.jpa.open-in-view=false

server.port=${SERVER_PORT:8080}
server.error.include-stacktrace=never
server.error.include-message=never

# Máxima distancia entre la entrada y el inicio de un turno para asociarlo automáticamente.
attendance.shift-match-window-minutes=${ATTENDANCE_SHIFT_MATCH_WINDOW_MINUTES:240}

horabase.jwt.secret=${JWT_SECRET}
horabase.jwt.issuer=${JWT_ISSUER:horabase-api}
horabase.jwt.expiration-minutes=${JWT_EXPIRATION_MINUTES:60}

horabase.password-reset.expiration-minutes=${PASSWORD_RESET_EXPIRATION_MINUTES:30}
horabase.password-reset.base-url=${PASSWORD_RESET_BASE_URL:http://localhost:3000/reset-password}
horabase.mail.from=${MAIL_FROM:no-reply@horabase.local}
horabase.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

spring.docker.compose.enabled=${SPRING_DOCKER_COMPOSE_ENABLED:false}

spring.mail.host=${SMTP_HOST:localhost}
spring.mail.port=${SMTP_PORT:1025}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:false}

horabase.bootstrap.enabled=${HORABASE_BOOTSTRAP_ENABLED:false}
horabase.bootstrap.business-name=${HORABASE_BOOTSTRAP_BUSINESS_NAME:}
horabase.bootstrap.tax-id=${HORABASE_BOOTSTRAP_TAX_ID:}
horabase.bootstrap.admin-document=${HORABASE_BOOTSTRAP_ADMIN_DOCUMENT:}
horabase.bootstrap.admin-email=${HORABASE_BOOTSTRAP_ADMIN_EMAIL:}
horabase.bootstrap.admin-password=${HORABASE_BOOTSTRAP_ADMIN_PASSWORD:}
